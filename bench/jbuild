(executables
 ((names (bench))
  (libraries (async
              core
              interval_map
              jenga_lib
              textutils_kernel.text_block))
  (modules (:standard \ analyze))
  (preprocess (pps (ppx_jane ppxlib.runner)))))



(jbuild_version 1)
