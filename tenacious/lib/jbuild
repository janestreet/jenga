(library
 ((name tenacious_lib)
  (public_name jenga.tenacious)
  (flags (:standard -safe-string))
  (libraries (core async))
  (preprocess (pps (ppx_jane ppxlib.runner)))))

;(alias ((name lib_artifacts) (deps ((alias runtest)))))


(jbuild_version 1)
