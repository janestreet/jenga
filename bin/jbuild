(embed
 ((names (jenga))
  (code_style ppx)
  (libraries (
    async
    async_kernel
    async_unix
    core
    core_kernel
    fieldslib
    jenga_lib
    parsexp
    re
    re.pcre
    sexplib
    ))
  (pps (ppx_jane))))

(executables
 ((names (jenga))
  (libraries (async core jenga_lib))))

(install
 ((section bin)
  (files ((jenga.exe as jenga)))))
